## Event

On jobber schedule if we wanna crate an event we see this popup which we already have `ref/event/1`. Now if we click on 'More options' we see a window open which is this `ref/event/2` where we can see: event title, event description, schedule start date, end date , start tiem, end time and anytime box. We already have in Job these, we can just reuse with existing logic which already applied in Job where the date auto sets and when we click on start time then auto end time sets by default later we can customize. Bottom we can see a repeat field with options which looks like this `ref/event/3` , we already have this in Job we will just reuse that with all realted features, nothing to crate again. Once we save an event we see it appears on calendar and if we click on that event card we see a info popup for that event which looks like this `ref/event/4`. We already have a Details popup so we will use that, content just renders on needs. Here we can see a 'Details' and 'Edit' button. We wont add any details button there instead we will add a 'Delete' button there to delete an event. 'Edit' button. If we click on 'Edit' button we see this window `ref/event/5` which is the same window `ref/event/2,3` just content popuplated and rendered.
