# Request

Here I am sharing what I see in Jobber and we will follow this

## Request and Booking form setting tab

When I click on this this window look like `ref/req/1.jpg`. The form preview window looks like this `ref/req/2.jpg`. After clicking the public url the whole Client journey looks like: `ref/req/3, 4, 5, 6, 7`.

## Request tab

From the Sidebar menu if we click on Request tab the route looks like: `ref/req/8.jpg`. We can see there are status. You can load your jobber skills and know about those status how they work and we will replicate.

## Details page

If we click on any request single item from the list then the details page looks like: `ref/req/9.jpg`. You can see all has block level edit option 'pencil icon' like we already kinda made for job page. same here.

## On site assesment

We can see there is a checkbox to complete. If we click on this this will mark as completed, in job page the way we mark a visit as completed. We can reuse that button or anything. When marking done then a popup shows like `ref/req/10.jpg`. This popup is optional. In our job page we can mark the visit as cmpleted and a optional popup appear. We can reuse that.

Now if we click on the Edit icon then this block looks like this: `ref/req/11.jpg`. I think we laready have these blokcs/fields in job page for visits and we can reuse here.

## New Request page

When I click on new request page then the new page looks like: `ref/req/12.jpg`. If we click the 'On site assessment' then the blocks turn into this again `ref/req/13.jpg` which is the same as `ref/req/11.jpg`.
